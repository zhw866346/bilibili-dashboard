# ==========================================================================
# 大模型 API Key 配置模板
# --------------------------------------------------------------------------
# 用法：把本文件复制成【同目录下的 .env】（文件名就叫 .env，没有 .example），
#      然后把等号后面填上你自己的 key。
#
# ★ .env 已被 .gitignore 排除，不会被提交到 GitHub。
# ★ 这个模板文件（.env.example）会入库 ——
#   所以这里【只能有键名，绝不能有值】。写下真 key 再提交就等于把 key 公开。
#
# 不配也能跑：只跑 `npm run dev` 时走的是关键词规则路径，功能完整，
# 只是不调用大模型。页面上会如实标出当前走的是哪条路。
# ==========================================================================

# DeepSeek（默认供应商）：中国大陆直连，不需要代理。
# 申请地址 https://platform.deepseek.com
DEEPSEEK_API_KEY=

# --------------------------------------------------------------------------
# 以下都是可选项，不填就用代码里的默认值。换供应商时才需要动。
# --------------------------------------------------------------------------

# 供应商。可选 deepseek / zhipu / moonshot，默认 deepseek。
# 三家的差异全部收在 server/providers.mjs 一个文件里。
# LLM_PROVIDER=deepseek

# 模型名。不填就用该供应商的默认模型。
# ★ 别在这里猜模型名 —— 先跑 `npm run server` 看启动日志，
#   它会调一次 /v1/models 把账号真实可用的模型列出来。
# LLM_MODEL=

# 后端监听端口，默认 8787（只绑 127.0.0.1，不会暴露到局域网）。
# PORT=8787

# 本机 Python 路径。不填会自动按顺序找
# $PYTHON → python → py。
# PYTHON=
