node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# 备份文件不入库，本地保留
*.bak

# 临时脚本不入库。跑一致性核对时会在根目录建 xxx.tmp.ts / xxx.tmp.mjs，
# 跑完即删；这两行是兜底——万一忘了删，git add -A 也不会把它们塞进仓库
*.tmp.ts
*.tmp.mjs

# 从 dataset.ts 导出的全量 CSV，约 21MB，由 `npm run data:export` 现场生成。
# 不入库——留着它就等于在仓库里放第二份数据，改种子时必然忘记同步，
# 而「两份数据不一致」正好是这个项目最怕的事。
# 给人看的小样本（data/sample/，各表前 1000 行）是入库的。
data/csv/

# 大模型 API Key 绝对不能入库。
# ★ 这是全项目唯一一条「一旦失手就无法挽回」的规则 ——
#   仓库在 GitHub 上，key 推上去就等于公开，只能作废重办。
#   注意 *.local 只覆盖 `.env.local`，覆盖不到 `.env` 本身，所以必须单独写这三行。
.env
.env.*
server/.env

# 但「模板」要入库 —— 它只有键名、没有值，是给下一个拿到这份代码的人看的。
# ★ 没有这一行例外，上面的 `.env.*` 会把模板一起吞掉，
#   表现是「我明明建了 .env.example，clone 的人却说找不到」。
!.env.example
!server/.env.example

# ★ 2026-09-15 作品集收尾：这一段原来在这里列着「本机自用」的那几个文件名，
#   现在【整段搬去了本机的 .git/info/exclude】。
#   理由：.gitignore 本身是入库的、公开的 —— 那份名单等于挂在 GitHub 上
#   告诉所有人「这个仓库藏了哪几个文件」。
#   搬过去之后，挡住 `git add -A` 的效果一模一样，而那个文件 GitHub 上永远看不到。
#   ★ 上面那三行 .env 的保险丝【留在这里不动】：clone 的人也用得上它，必须公开。
#   ★ 那些文件一个都没删，本机照旧能双击、能读。
